#include <stdio.h>

int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        long long int x, n;
        scanf("%lld%lld", &x, &n);
        if (n>4)
        {        
        for (int i = 0; i < n; i++)
        {
            n/=4;
        }}else
        {
            
        }
        
        
    }
    return 0;
}